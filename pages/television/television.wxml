<view class="television">
  <block wx:for="{{list}}" wx:key="{{item.id}}">
    <view class="item">
      <video class="itemVideo" src="{{item.src}}"   controls ></video>
    </view>
  </block>
</view>
